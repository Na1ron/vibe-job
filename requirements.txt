# audio
sounddevice>=0.4.6
numpy>=1.23
pycaw>=20240123

# whisper
openai-whisper
torch>=2.0
tqdm

# VAD (optional, fallback = energy VAD)
webrtcvad-wheels; platform_system == "Windows"

# LLM (REQUIRED)
groq
